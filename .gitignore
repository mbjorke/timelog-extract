.venv/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
build/
*.egg-info/

# Local project config (copy from timelog_projects.example.json)
timelog_projects.json
timelog_projects.json.marcus
timelog_projects.backup.*.json
decisions-*.json
timelog_projects.json.*.bak
timelog_projects.home-backup-*.json
timelog_projects.repo-backup-*.json

# Local logs
.remote.env
TIMELOG.md

# macOS metadata
.DS_Store

# Local business / pitch notes (never commit)
private/
# Keep .claude/ local, but track only the shared repo skill manifests
# (thin SKILL.md wrappers → docs/skills/). Everything else under .claude/ stays local.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/*/
.claude/skills/*/*
!.claude/skills/*/SKILL.md
.tmp/
.wrangler/

# Generated reports/artifacts
output/
# CLI default report dir (`gittan report --json-file out/...`, reconciliation scripts, CI uploads under out/cli-experiments/)
out/
*.cast

# Briox API secrets (see .env.briox.example)
.env.briox

# Extension dependencies
cursor-extension/node_modules/
